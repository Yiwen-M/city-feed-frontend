import { styled } from '@mui/system';

import Card from '@mui/material/Card';

const WrapperCardStyled = styled(Card)({
  paddingTop: '10px',
  paddingLeft: '80px',
  paddingRight: '80px',
  marginBottom: '80px',
  boxShadow: '10px 10px 20px rgba(0, 0, 0, 0.3)',
});

export { WrapperCardStyled };
